{
  "$schema": "http://json-schema.org/schema",
  "version": 1,
  "title": "Deploy executor",
  "description": "",
  "type": "object",
  "properties": {
    "sshTarget": {
      "type": "object",
      "properties": {
        "target": {
          "type": "string",
          "description": "Name of the SSH Target"
        },
        "user": {
          "type": "string",
          "description": "Username on the target"
        }
      },
      "required": ["target", "user"],
      "description": "The SSH target available from the SSH config to deploy to."
    },
    "affectedServices": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "An array of strings with systemd services to stop before deploying files and restart after",
      "default": []
    },
    "paths": {
      "type": "object",
      "properties": {
        "origin": {
          "type": "string",
          "description": "The origin path to deploy from."
        },
        "destination": {
          "type": "string",
          "description": "The destination path to deploy to."
        }
      },
      "required": ["origin", "destination"],
      "description": "Theses are the actual paths that will be deployed."
    }
  },
  "required": ["sshTarget", "paths"]
}
